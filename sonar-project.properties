# Required basic settings
sonar.projectKey=Techytobii_introduction-to-ci-cd
sonar.organization=techytobii
sonar.host.url=https://sonarcloud.io

# Source code to analyze
sonar.sources=.

# Exclude non-source directories (optional but recommended)
sonar.exclusions=**/node_modules/**,**/*.test.js

# JavaScript code coverage report
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Optional: set a dynamic version (if passed from GitHub Actions)
# sonar.projectVersion=1.0.${{ github.run_number }}
